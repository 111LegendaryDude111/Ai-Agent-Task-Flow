# Verification Artifacts

Task CLI evidence is stored under `.tmp/tasks/{feature}/` in task/subtask JSON fields.

Project-level verification artifacts can be added here later if the selected provider/workflow needs persistent reports.
